angular.module('arraysApp')
    .controller('PageMarkdownCtrl', ['$scope', '$state', '$q', 'AuthService', '$mdToast', '$window', '$rootScope',
        function ($scope, $state, $q, AuthService, $mdToast, $window, $rootScope) {
        }
    ]);
